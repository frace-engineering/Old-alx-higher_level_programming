#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    if key in a_disctionary:
        a_dictionary[key] = new_value
    else:
        a_dictionary.update({key:value})
    return a_dictionary
